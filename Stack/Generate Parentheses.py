def generateParenthesis(n: int) -> list[str]:
    # Create a counter for however many parenthesis we have left
    open = n - 1
    close = n
    result = []
    
    recursiveParenthesis
    

def recursiveParenthesis(open: list[str], close: list[str], input: str) -> list[str]:
    pass
        